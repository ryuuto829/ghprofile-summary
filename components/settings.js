// Number of user's public repos requested from the Github API
export const MAX_REPOS_ITEMS = 10

export const DEFAULT_TITLE = 'GHProfile Summary'
export const DEFAULT_DESCRIPTION = `Find your GitHub profile and \
look it's summary with charts`
